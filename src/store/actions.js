import { getCategories } from "./directory/directory-actions";
import { getProducts } from "./shop/shop-actions";

export const actions = {
  getCategories,
  getProducts,
};

export default actions;
